# Copy this file to `.env` and fill in your values.
# Bun auto-loads `.env` from the project root on boot — no dotenv import needed.

# API key for endpoints with `auth: true`. If unset, those endpoints reject all requests.
API_KEY=change-me

# Port the server listens on. Default 47291 (matches the original repo).
PORT=47291
